export const LINK_PRIVACY_POLICY = '';
export const LINK_SUPPORT = '';

export function assertProductionLinks() {
  // if (!__DEV__ && (!LINK_PRIVACY_POLICY || !LINK_SUPPORT)) {
  //   throw new Error(
  //     'LINK_PRIVACY_POLICY and LINK_SUPPORT are required in production.',
  //   );
  // }
}
